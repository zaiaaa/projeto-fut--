let lmt = 100
let sld_atl = 100

let r = (sld_atl < lmt) ? "Você ainda pode depositar dinheiro" : "Seu limite foi atingido.";


alert(r)

//let variável = (condição) ? //instruçao se true: // Instrução se false